<h1>Users</h1>

<%= @users.each do |user| %>
<p>image：<%= image_tag user.user.get_profile_image(100,100) %></p>


  <p>Users</p>
  <span><%= book.title %></span>
  
<% end %>
<div>

</div>



<!-- ユーザーの投稿一覧 -->
<% @users.each do |user %>
<p>投稿ユーザー画像：<%= image_tag post_image.user.get_profile_image(100,100) %></p>



<div>
 <p>image：<%= user.title %></p>
  <p>本文：<%= post_image.body %></p> 
</div>
<% end %>


<h1>New book</h1>
<!-- form_with部分 -->
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
	
  <h4>Opinion</h4>
  <%= f.text_area :body %>
	
  
  <%= f.submit "Create Book" %>

<% end %>